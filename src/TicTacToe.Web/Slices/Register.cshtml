@inherits RazorSliceHttpResult<(string Title, string? Error)>
@implements IUsesLayout<_Layout, LayoutModel>

@await RenderPartialAsync(_RegisterForm.Create(Model))

@functions {
    public LayoutModel LayoutModel => new(Model.Title);
}
