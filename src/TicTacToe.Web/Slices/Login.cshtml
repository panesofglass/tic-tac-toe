@inherits RazorSliceHttpResult<(string Title, AntiforgeryTokenSet Token, string? Error)>
@implements IUsesLayout<_Layout, LayoutModel>

@await RenderPartialAsync(_LoginForm.Create(Model))

@functions {
    public LayoutModel LayoutModel => new(Model.Title);
}
