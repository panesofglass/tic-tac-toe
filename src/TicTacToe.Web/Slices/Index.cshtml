@inherits RazorSliceHttpResult<(List<GameModel> Games, Player? Player)>
@implements IUsesLayout<_Layout, LayoutModel>

@await RenderPartialAsync(GameList.Create(Model.Games))

@functions {
    public LayoutModel LayoutModel => new()
    {
        Title = "Games",
        CurrentPlayer = Model.Player
    };
}
